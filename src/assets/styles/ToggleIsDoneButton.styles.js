import {StyleSheet} from 'react-native';

const ToggleIsDoneButtonStyles = StyleSheet.create({
  button: {
    borderRadius: 30,
    width: 25,
    height: 25,
    elevation: 11,
    position: 'absolute',
    alignItems: 'center',
    justifyContent: 'center',
    top: 16,
    right: 16,
  },
});

export default ToggleIsDoneButtonStyles;
