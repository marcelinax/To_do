import React from 'react';
import {StyleSheet} from 'react-native';

const AddTaskButtonStyles = StyleSheet.create({
  button: {
    width: 90,
    height: 90,
    borderRadius: 100,
    backgroundColor: '#5AA9E2',
    position: 'absolute',
    zIndex: 2,
    bottom: 120,
    left: '43%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default AddTaskButtonStyles;
